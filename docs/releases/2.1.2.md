# Tavern 2.1.2

本次重点：恢复旧 MVU 卡在额外模型解析下的变量更新协议兼容性，避免 Nora 将所有非 Gemini / Claude 模型强制改成 JSONPatch-only 输出。

- MVU 额外模型请求重新使用当前卡片/上游的变量更新任务，不再替旧卡硬塞统一 JSONPatch 协议。
- 保留额外模型超时、重试、快照回滚和显式空 JSONPatch no-op 处理，避免回退稳定性保护。
- 提升 Nora MVU bundle 缓存版本，确保用户更新后加载新的运行包。
- 更新契约测试，防止后续再次引入全局强制 JSONPatch-only 逻辑。

更新命令：

```sh
curl -fsSL https://github.com/LoveMaker-art/noras-tavern/releases/latest/download/install-tavern-updater.sh | sh -s -- --apply --confirm
```

安装完成后，在 ClawChat 对话中发送 `/restart`，重新加载 MCP、技能和 AGENTS。
