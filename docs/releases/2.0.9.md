# Tavern 2.0.9

本次重点：修复旧 Python 数据迁移到 Nora Tavern 2.x 后，部分 MVU 卡片重新初始化失败的问题。

- Python 数据迁移现在会为 SillyTavern 世界书设置生成合法的 `globalSelect` 空数组。
- 世界书运行时会兼容已经迁移但缺少 `globalSelect` 的旧设置，不再把 `undefined` 传给 MVU。
- 修复 MVU Beta 初始化时出现的 `Spread syntax requires ...iterable not be null or undefined`。
- 已使用“电锯人”世界真实复测：MVU 初始化事件正常触发，变量数据和 schema 成功写入聊天记录。

更新命令：

```sh
curl -fsSL https://github.com/LoveMaker-art/noras-tavern/releases/latest/download/install-tavern-updater.sh | sh -s -- --apply --confirm
```

安装完成后，在 ClawChat 对话中发送 `/restart`，重新加载 MCP、技能和 AGENTS。
