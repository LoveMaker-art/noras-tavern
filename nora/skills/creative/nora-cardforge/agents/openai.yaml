interface:
  display_name: "Nora CardForge"
  short_description: "Create World cards; assess, polish and import cards"
  default_prompt: "Use $nora-cardforge to create a Nora World card from my brief and report what has been checked and what still needs runtime testing."

policy:
  allow_implicit_invocation: true
